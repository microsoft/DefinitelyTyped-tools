{
  "name": "definitely-not-slow",
  "version": "1.0.1",
  "description": "",
  "bin": "index.js",
  "scripts": {
    "build": "tsc -b .",
    "cli": "node dist/src/cli/index.js",
    "push-production": "npm run build && npm version patch && git commit -am 'Bump version' && git checkout production && git merge -m 'Merge master into production' master && npm run build && git add dist && git commit -m 'Update dist' && git push -u origin production && git checkout -"
  },
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^11.13.12"
  },
  "dependencies": {
    "@azure/cosmos": "^2.1.5",
    "@octokit/rest": "^16.27.0",
    "dtslint": "^0.7.7",
    "markdown-table": "^1.1.3",
    "types-publisher": "github:microsoft/types-publisher#production",
    "typescript": "^3.5.1"
  }
}
